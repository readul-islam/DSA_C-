#include <iostream>
using namespace std;

int main()
{
    int n = 1;
    while (true)
    {
        switch (n)
        {
        case 1:
            cout << 'P' << endl;

            exit(0); // will terminate loop process
        }
    }
}